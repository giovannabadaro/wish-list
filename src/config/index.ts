const usernameAdm: string = process.env.USER_NAME_ADM as string;
const passwordAdm: string = process.env.PASSWORD_ADM as string;
const secret: string = process.env.SECRET as string;

export { usernameAdm, passwordAdm, secret };
